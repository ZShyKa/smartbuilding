version: "3.8"

services:
  api:
    build:
      context: ./api
    container_name: smart-apartment-api
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
